<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="APP_NAME" value="order-service"/>
    <property name="LOG_PATH" value="logs"/>

    <!-- Console: keep general framework/startup logs here -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Business log file: only your package -->
    <appender name="BUSINESS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-business.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-business.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger: console only -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Your business/application package: file only -->
    <!-- Business logic packages ONLY -->
    <logger name="com.dkghosh.orderservice.controller" level="INFO" additivity="false">
        <appender-ref ref="BUSINESS_FILE"/>
    </logger>

    <logger name="com.dkghosh.orderservice.service" level="INFO" additivity="false">
        <appender-ref ref="BUSINESS_FILE"/>
    </logger>

    <logger name="com.dkghosh.orderservice.StartupLogger" level="INFO" additivity="false">
        <appender-ref ref="BUSINESS_FILE"/>
    </logger>

</configuration>